const findMatches = () => {
  // filter through winning numbers, use array.includes to
};

const calcPoints = () => {
  //from a list of matching numbers count length
  // return sum of 2 to the power of length
};
